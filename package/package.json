{
  "name": "package",
  "version": "1.0.0",
  "description": "Builds and bundles the package for distribution",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node ./build.js && npx webpack --config ./webpack.config.js"
  },
  "author": "Howard Chen",
  "license": "ISC",
  "dependencies": {
    "prettier": "^2.0.5"
  },
  "devDependencies": {
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.11"
  }
}
